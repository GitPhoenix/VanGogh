<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:orientation="vertical">

    <ListView
        android:id="@+id/lv_dialog_item"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@color/default_divider_line"
        android:dividerHeight="@dimen/dp_1"
        android:scrollbars="none"
        android:listSelector="@drawable/selector_item_bg"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_8"
        android:background="#f8f8f8"/>

    <TextView
        android:id="@+id/tv_dialog_item_cancel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/dp_16"
        android:background="@drawable/selector_item_bg"
        android:gravity="center"
        android:text="取消"
        android:textColor="@color/default_text_color_normal"
        android:textSize="18sp"/>

</LinearLayout>