<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.alley.van">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application>
        <activity android:name=".activity.VanMediaActivity"></activity>

        <activity android:name=".activity.VanFiltratePreviewActivity"></activity>

        <activity android:name=".activity.VanCropActivity"></activity>

        <activity android:name=".activity.VanPreviewActivity"></activity>
    </application>

</manifest>
